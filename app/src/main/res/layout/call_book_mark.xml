<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="3dp"
    android:background="@drawable/rounded_listview"
    android:foreground="?attr/selectableItemBackground"
    android:padding="5dp">

    <ImageView
        android:id="@+id/imv_bookmarkperson"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:background="@drawable/white_round10"
        android:padding="2dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/profileimg" />

    <TextView
        android:id="@+id/et_callbookName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_toEndOf="@+id/imv_bookmarkperson"
        android:fontFamily="@font/cafe24"
        android:textColor="#E6000000"
        android:textSize="20sp"
        android:textStyle="normal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/imv_bookmarkperson"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="막내 아들" />
</androidx.constraintlayout.widget.ConstraintLayout>